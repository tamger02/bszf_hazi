\doxysection{RAMFUNC -\/ RAM Function Support}
\hypertarget{group__ramfunc}{}\label{group__ramfunc}\index{RAMFUNC -\/ RAM Function Support@{RAMFUNC -\/ RAM Function Support}}


RAM code support.  


RAM code support. 

Provides support for executing code from RAM. Provides a unified method to manage RAM code across all supported tools.

\begin{DoxyNote}{Note}
Other cross-\/compiler support macros are implemented in \href{../../common/api/group-common}{\texttt{ COMMON}}.

Functions executing from RAM should not be declared as static.
\end{DoxyNote}
\begin{DoxyWarning}{Warning}
Standard library facilities are available to the tool with GCC in hosted mode (default), regardless of the section attribute. Calls to standard libraries placed in the default section may therefore occur. To disable hosted mode, add \textquotesingle{}-\/ffreestanding\textquotesingle{} to the build command line. This is the only way to guarantee no calls to standard libraries with GCC. Read more at www.\+gcc.\+gnu.\+org/onlinedocs/gcc-\/5.3.\+0/gcc/\+Standards.html

Keil/\+ARM u\+Vision users must add a section named "{}ram\+\_\+code"{} in their linker scatter file. This section must be in RAM memory. Look in the MCU SDK for example scatter files (ram\+\_\+code.\+sct).
\end{DoxyWarning}
~\newline
 \hypertarget{group__ramfunc_ramfunc_usage}{}\doxysubsection{\texorpdfstring{Usage}{Usage}}\label{group__ramfunc_ramfunc_usage}
In your .h file\+: \begin{DoxyVerb}#include "em_ramfunc.h"

SL_RAMFUNC_DECLARATOR
void MyPrint(const char* string);
\end{DoxyVerb}


Issues have been observed with ARM GCC when there is no declarator. It is recommended to have a declarator also for internal functions but move the declarator to the .c file.

In your .c file\+: \begin{DoxyVerb}#include "em_ramfunc.h"

SL_RAMFUNC_DEFINITION_BEGIN
void MyPrint(const char* string)
{
...
}
SL_RAMFUNC_DEFINITION_END
\end{DoxyVerb}
 